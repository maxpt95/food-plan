[project]
name = "food-plan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
]

[tool.pytest]
minversion = "9.0"
pythonpath = ["src"]
testpaths = ["tests", "src"]
addopts = [
    "--import-mode=importlib",
]
